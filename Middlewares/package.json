{
  "dependencies": {
    "express": "^5.1.0"
  },
  "name": "middlewares",
  "version": "1.0.0",
  "description": "Middlewares in express are function that come into play after the server receives the request and before the response is sent to the client.",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
